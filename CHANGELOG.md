# 更新日志

本项目所有值得注意的变更都记录在此文件。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.1.0] - 2026-09-19

### 新增

- **动态视角摇晃**：行走时相机产生垂直起伏、水平摆动与沿视线轴的轻微倾斜。相位按行进距离推进而非时间，因此步频随移速自然变化；静止、飞行或游泳时强度平滑淡出，不会出现相机突跳。`B` 键可随时开关。
- **方块破坏粒子**：破坏方块时爆出 24–32 个碎屑。每片碎屑的 UV 取自被破坏方块侧面纹理的随机小区域，因此颜色花纹与原方块一致；带有重力、空气阻力、分轴 AABB 碰撞（落地停住并轻微反弹）以及生命末段的淡出。
- 命令行参数 `--walk`（自动前进）与 `--dig`（自动破坏准星所指方块），配合既有的 `--shot <帧数>` 可用于无人值守的效果验证与录屏。
- 调试面板新增内容：视角摇晃开关状态、当前摇晃强度与倾角、存活粒子数。

### 变更

- `.gitignore` 补充忽略 `obj/` 构建产物目录。

### 修复

- 修正玩家近乎垂直俯视时，粒子 billboard 的相机右/上向量叉积退化导致的坐标 NaN。

## [1.0.0] - 2026-09-19

首个可运行版本。

### 新增

- **外置 PNG 材质系统**：30 张方块面材质从可执行文件同级的 `materials/` 目录加载，推荐 16×16，其它尺寸以最近邻缩放以保留像素画硬边。首次运行自动将内置材质导出为 PNG 供编辑；删除某个 PNG 即回退到该材质的内置生成版本并在下次启动重新导出。
- **世界生成**：大陆、丘陵、细节三层噪声叠加的地形；平原 / 森林 / 沙漠 / 雪原四种生物群系；溶洞与隧道两种洞穴形态；按深度分层的煤、铁、金、钻石矿脉；可跨区块边界的树木。
- **区块系统**：16×128×16 区块，生成与网格构建在后台线程池完成，按到玩家的距离排队，超出视距后卸载并释放 GPU 资源。
- **区块渲染**：被遮挡面剔除、逐顶点环境光遮蔽烘焙、四边形对角线按 AO 差异自适应翻转、视锥剔除、与天空一致的距离雾。
- **透明渲染**：水、玻璃、冰走独立混合通道并由远及近排序；树叶使用 alpha 裁剪保留深度写入。
- **玩家**：重力与 AABB 碰撞求解、疾跑、潜行、游泳、创造飞行；三轴分步推进以正确处理贴墙滑行与落地。
- **交互**：方块破坏、放置与中键拾取，选中方块线框高亮。
- **界面**：程序化 5×7 像素字体、十字准星、9 格快捷栏与物品名称提示、`F3` 调试面板。
- **昼夜循环**：天空渐变、太阳圆盘与光晕、黄昏色调、随时间变化的环境光强度。
- **截图**：`F2` 将当前帧缓冲写出为 BMP 文件。

### 修复

- 修正矩阵组合顺序与 uniform 上传的转置参数，此前世界被投影到错误的裁剪空间。
- uniform 写入统一改用 `GL.ProgramUniform*` 并显式传入程序句柄，修复多着色器切换场景下的参数错写。
- 修正图集写入的 Y 轴方向（内存首行是图像顶部，而 OpenGL 纹理首行是 `v = 0`），此前 UV 全部采样到 Padding 空白区，方块被 alpha 裁剪整体丢弃。
- `GpuMesh` 的 VAO/VBO 改为延迟到主线程首次上传时创建，修复后台线程无 GL 上下文时创建失败、导致网格"已绘制但不可见"的问题。
